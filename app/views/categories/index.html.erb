<h1>Categorias</h1>

<%= link_to "Nova Categoria", new_category_path %>


<div id="categories">
    <% @categories.each do |category| %>
    <div>
        <%= link_to category.name, category %>
    </div>
    <% end %>
</div>