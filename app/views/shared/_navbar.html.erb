<nav class="bg-blue-600 text-white px-6 py-4 shadow-md fixed top-0 left-0 w-full z-10">
    <div class="container mx-auto flex justify-between items-center">
        <%= link_to "To-Do List", root_path, class: "text-2xl font-bold hover:underline" %>

        <div class="flex items-center gap-8">
            <% if Current.session %>
            <p class="font-medium">Olá, <%= Current.session.user.name %></p>
            <%= button_to "Sair", session_path, method: :delete, 
            class: "bg-red-500 px-3 py-2 rounded hover:bg-red-600 transition-all font-medium" %>
            <% else %>
            <%= link_to "Entrar", new_session_path, class: "hover:underline" %>
            <%= link_to "Cadastrar", new_user_path, 
            class: "bg-green-500 px-3 py-2 rounded hover:bg-green-600 transition-all font-medium" %>
            <% end %>
        </div>
    </div>
</nav>